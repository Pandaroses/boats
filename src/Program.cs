
using VesselFeud;

namespace VesselFeud {
    internal class Program {
        static void Main(string[] args) {
            Game meow = new Game();
            meow.init();
            // //TODO add to game
            // player.init_grid();
            // enemy.init_grid();
            // player.place_ships(ships);
            // enemy.place_ships(ships);
            // while (true) {
            //     var coords = player.Turn();
            //     var result = enemy.Attack(coords.Item1, coords.Item2);
            //     player.result(result, coords.Item1, coords.Item2);
            //     player.render_grids();
            // }

        }


    }
}
